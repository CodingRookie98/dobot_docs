**Description: 获取设备UID**

**KeyWords:**

portName: string

deviceID: array(int)

 

INPUT:

``` language-json
{
    "id": 1,
    "jsonrpc": "2.0",
    "method": "dobotlink.MagicBox.GetDeviceID",
    "params": {
        "portName": "COM4"
    }
}
```