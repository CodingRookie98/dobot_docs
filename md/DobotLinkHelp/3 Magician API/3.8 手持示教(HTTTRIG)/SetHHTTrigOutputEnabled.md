### 3.8.3 SetHHTTrigOutputEnabled

**KeyWords:**

portName: string

enable: bool

*isQueued: bool (default:false)

INPUT:

```json
{
    "id": 1,
    "jsonrpc": "2.0",
    "method": "dobotlink.Magician.SetHHTTrigOutputEnabled",
    "params": {
        "portName": "COM4",
        "enable": true
        "isQueued": false
    }
}
```
