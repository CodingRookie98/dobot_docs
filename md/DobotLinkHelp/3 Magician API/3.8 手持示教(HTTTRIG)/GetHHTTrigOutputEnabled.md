### 3.8.4 GetHHTTrigOutputEnabled

**KeyWords:**

portName: string

isEnabled: bool

INPUT:

```json
{
    "id": 1,
    "jsonrpc": "2.0",
    "method": "dobotlink.Magician.GetHHTTrigOutputEnabled",
    "params": {
        "portName": "COM4"
    }
}
```
