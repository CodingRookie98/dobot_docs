### 3.9.7 SetEndEffectorSuctionCup

**KeyWords:**

portName: string

enable: bool

on: bool

*isQueued: bool (default:false)

INPUT:

```json
{
    "id": 1,
    "jsonrpc": "2.0",
    "method": "dobotlink.Magician.SetEndEffectorSuctionCup",
    "params": {
        "portName": "COM4",
        "enable": true,
        "on": true,
        "isQueued": false
    }
}
```
