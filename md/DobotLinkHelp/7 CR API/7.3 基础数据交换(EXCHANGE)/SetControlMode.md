### 7.3.3 SetControlMode

**KeyWords:**

portName: string

mode: string ('disable', 'enable', 'drag')

INPUT:

```json
{
    "id": 1,
    "jsonrpc": "2.0",
    "method": "dobotlink.CR.SetControlMode",
    "params": {
        "portName": "192.168.5.1",
        "mode": "enable"
    }
}
```
